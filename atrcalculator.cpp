// atrcalculator.cpp
#include "atrcalculator.h"

double ATRCalculator::calculateATR(const QString& symbol, int period)
{
    // 这里实现ATR计算逻辑
    // 示例中返回随机值作为ATR
    return (qrand() % 100) / 10.0;
}
